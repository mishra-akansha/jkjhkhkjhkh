# Frontend routes

Page components live in this folder. Routes are registered explicitly in
`src/router.jsx`, so adding a page requires creating the component here and
adding it to that route tree.

Current routes:

- `/` — landing page
- `/login` — sign in
- `/signup` — registration
- `/dashboard` — authenticated workspace
